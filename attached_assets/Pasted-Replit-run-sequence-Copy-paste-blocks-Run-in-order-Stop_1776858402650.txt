Replit run sequence
Copy-paste blocks. Run in order. Stop at any failure and report back.

1. Pull + apply schema
git pull origin master
pnpm install
pnpm --filter @workspace/db run test:schema
Expect: clean

for all of the prs in upshift -data

Replit runbook — all PRs from this session
15 PRs to verify. Copy-paste blocks in order. Stop on any error and report back.

0. Pull + install
cd ~/workspace
git checkout master
git pull origin master
git log --oneline -20
pnpm install
Expect: log shows #49 → #72. pnpm install finishes clean.

1. Pre-flight check BEFORE schema push (PR #52)
The events_source_enum migration will fail if any row has a source value outside the allowed set. Check first:

psql "$DATABASE_URL" -c "
SELECT DISTINCT source FROM events
UNION SELECT DISTINCT source FROM matches
UNION SELECT DISTINCT source FROM event_teams
UNION SELECT DISTINCT source FROM club_roster_snapshots;
"
Expect: subset of {gotsport, sincsports, manual, other, NULL}. If anything else appears, STOP — fix data first or extend the enum before pushing.

2. Schema push (PRs #52, #57, #71)
pnpm --filter @workspace/db run test:schema
pnpm --filter @workspace/db run push
Drizzle prompts to expect (answer in order):

events_source_enum new enum → Yes, create
events.source / event_teams.source / matches.source / club_roster_snapshots.source → alter type (NOT rename)
Drop check events_source_enum constraint → Yes
Recreate events_source_platform_id_uq + matches_source_platform_id_uq → Yes
New table player_id_selections → Yes, create
New columns canonical_clubs.manually_merged, club_aliases.merged_from_canonical_id, club_aliases.merged_at → Yes, add (NOT rename)
3. Verify schema push
psql "$DATABASE_URL" -c "
SELECT enum_range(NULL::events_source_enum);
SELECT count(*) FROM player_id_selections;
SELECT column_name FROM information_schema.columns WHERE table_name='canonical_clubs' AND column_name='manually_merged';
SELECT column_name FROM information_schema.columns WHERE table_name='club_aliases' AND column_name IN ('merged_from_canonical_id','merged_at');
"
Expect: enum {gotsport,sincsports,manual,other}, player_id_selections count 0, all 3 columns present.

4. Verify scheduled jobs (PRs #49, #51, #53, #70)
In Replit dashboard → Deployments → Scheduled Jobs, you should see 4:

retention-prune 02:00 UTC
nightly-canonical-club-linker 03:00 UTC
daily-club-results-rollup 04:00 UTC
daily-scrape-health-rollup 05:00 UTC
Smoke test one cron now (won't wait for next firing):

cd scraper
python3 run.py --source link-canonical-clubs --dry-run --limit 50
python3 run.py --rollup retention-prune --dry-run
python3 run.py --rollup scrape-health --dry-run
python3 run.py --rollup club-results --dry-run
Expect: all four print sane summaries; no exceptions.

5. Audit Google Maps state seeds (PR #50)
cd ~/workspace/scraper
python3 audits/audit_state_maps.py
Expect: 6 rows (CT, Eastern PA, IN, MO, TN, TX-South). Note any non-OK rows for follow-up.

6. Smoke-test the 3 new club extractors (PRs #58, #63, #66)
cd ~/workspace/scraper
python3 run.py --source squarespace-clubs --dry-run --limit 5
python3 run.py --source sportsengine-clubs --dry-run --limit 5
python3 run.py --source duda-360player-clubs --dry-run --limit 5
For each: note how many candidate clubs were detected, how many yielded JSON-LD, what data types surfaced (rosters / coaches / events). If one yields nothing useful, that's a tuning signal — report back.

PR #61 (youthsoccersports) is a skip recommendation; nothing to run.

7. NPL re-enable smoke test (PR #56)
cd ~/workspace/scraper
python3 run.py --league "Ohio Valley NPL"
Expect: ≥15 clubs in scraper/output/. If 0 → re-check event_id in scraper/data/leagues_master.csv. If <5 → consider downgrading scrape_priority back to low.

8. US Club iD scraper (PRs #57, #72)
cd ~/workspace/scraper
python3 run.py --source usclub-id --dry-run
Expect: discovers SoccerWire iD-pool / training-center articles, parses player rows from article bodies. If non-zero rows produced, run for real:

python3 run.py --source usclub-id
psql "$DATABASE_URL" -c "SELECT pool_tier, count(*) FROM player_id_selections GROUP BY pool_tier;"
9. Dedup tooling — most careful (PR #71)
9a. Dry-run first, ALWAYS
cd ~/workspace/scraper
python3 run.py --source club-dedup-resolve --dry-run
ls -la scraper/dedup/output/
Expect: prints tier counts (auto-merge / review / skip), writes dedup-review-<date>.csv. Eyeball the CSV before doing anything destructive.

9b. Backup BEFORE committing
pg_dump "$DATABASE_URL" \
  -t canonical_clubs -t club_aliases -t club_affiliations \
  -t coach_discoveries -t event_teams -t matches -t player_id_selections \
  -t club_roster_snapshots -t roster_diffs -t tryouts -t club_results \
  -t coach_scrape_snapshots -t club_site_changes -t coach_career_history \
  > pre-dedup-merge-$(date +%Y%m%d).sql
ls -la pre-dedup-merge-*.sql
Expect: dump file >100KB.

9c. Commit auto-merge tier
python3 run.py --source club-dedup-resolve --no-dry-run
psql "$DATABASE_URL" -c "
SELECT count(*) AS merges FROM club_aliases WHERE source = 'merger-auto';
SELECT alias_name, merged_from_canonical_id, merged_at
FROM club_aliases WHERE source = 'merger-auto'
ORDER BY merged_at DESC LIMIT 20;
"
Expect: merges count matches the auto-merge tier from step 9a. Audit rows visible.

9d. Rollback if needed
psql "$DATABASE_URL" < pre-dedup-merge-<date>.sql
10. Tomorrow morning check — cron observability
After the crons have had a chance to fire overnight:

psql "$DATABASE_URL" -c "
SELECT scraper_key, status, started_at, records_touched
FROM scrape_run_logs
WHERE scraper_key LIKE 'rollup:%' OR scraper_key = 'source:link-canonical-clubs'
ORDER BY started_at DESC LIMIT 20;
"

psql "$DATABASE_URL" -c "
SELECT entity_type, status, count(*)
FROM scrape_health
GROUP BY entity_type, status
ORDER BY entity_type, status;
"
Expect: 4 fresh rollup rows (one per cron), scrape_health populated with status distribution.

Done
Report back with:

Any failures or unexpected output from steps 1-10
Tier counts from dedup dry-run (step 9a)
Tomorrow's cron + scrape_health output (step 10)
That signal drives the next round of code work.