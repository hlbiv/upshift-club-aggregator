You are shipping a Phase 0 probe of the US Soccer Learning Center directory.

**Target:** https://learning.ussoccer.com/directory — Angular SPA on S3+CloudFront. The goal is to answer: can a shell-level fetch client (no browser, no JS) extract directory/coach data, and if not, what specifically blocks it?

**Repo:** hlbiv/upshift-data. Start from fresh master. Branch name: probe/ussf-learning-directory.

**Escalation tree (follow strictly; stop at whichever step yields usable data OR at step 4):**

1. **Naive fetch** — fetch("https://learning.ussoccer.com/directory"). Record status, size, whether body is a hydrated HTML page or a ~3KB Angular shell.
2. **Realistic UA + headers** — retry with a real-Chrome User-Agent and Accept: text/html,.... Record whether the response changes.
3. **Bundle / inlined-JSON inspection** — fetch the HTML shell and look for:
   - <script>window.__INITIAL_STATE__ = ... / window.__PRELOADED_STATE__
   - Angular environment.apiBaseUrl leaked in a <script src=...> bundle (fetch those JS bundles and grep)
   - Any /api/*, /directory/*, GraphQL endpoints referenced. If any are found, try fetching them with headers and capture the response shape (JSON? 401? HTML?).
4. **STOP** — do NOT install or introduce Playwright, Puppeteer, or any headless browser. If steps 1–3 all fail to surface data, the probe's finding is "requires full browser rendering; decision escalated to user." Document exactly what you saw at each step so the user can make the Playwright-in-TS call in a later session.

**Deliverables (must ship in the probe PR):**

1. **scripts/src/probe-ussf-directory.ts** — TypeScript probe script using native fetch (no deps added). Mirror the pattern of scripts/src/probe-hudl-fan.ts (read that file first to copy style, arg parsing, JSON-report emission, exit-code semantics). The script should execute the 1–3 escalation steps and write a JSON report to scripts/output/ussf-directory-probe-<timestamp>.json.

2. **scripts/package.json** — add a "probe-ussf-directory" npm script entry that runs the probe via tsx. Match the pattern already there for probe-hudl-fan.

3. **docs/design/ussf-phase-0-probe-report.md** — follow the template in docs/design/hudl-phase-0-probe-report.md exactly. Required sections:
   - **Status** (Complete / Blocked / Partial)
   - **TL;DR** (2–3 sentences)
   - **Run metadata** (ran-at, Node version, host, invocation, UA used, JSON report path)
   - **Per-URL results** (status code, elapsed ms, body size, classifier [html-shell / json / blocked / …], cache headers)
   - **Response-header fingerprint** (CDN + bot-wall inference — CloudFront? Cloudflare? WAF? Akamai?)
   - **HTML shape** (a short snippet of the body + interpretation — is it the Angular shell? What JS bundles are referenced?)
   - **Implications** (what's greenlit for Phase 1, what's still unknown, open questions)
   - **Related** (links to adjacent docs — Hudl probe, egress docs)

**Hard constraints:**

- Do NOT add any dependency to package.json or scripts/package.json beyond what's already installed. No Playwright. No Puppeteer. No undici. Native fetch only.
- Do NOT run scrapers. Do NOT run pnpm --filter @workspace/db run push. Nothing that touches the DB.
- Do NOT modify files outside scripts/src/, scripts/package.json, and docs/design/.
- The probe MAY be run locally to capture real data — this is read-only recon against a public web page. Run from your local host, capture the JSON output, commit it alongside the report.
- Typecheck before commit: pnpm typecheck from repo root. Must pass.

**Workflow:**

1. git fetch origin master && git checkout -b probe/ussf-learning-directory origin/master
2. Read scripts/src/probe-hudl-fan.ts and docs/design/hudl-phase-0-probe-report.md first — those are your templates.
3. Write the probe script.
4. Run it locally. Commit the JSON output to scripts/output/ (or wherever the hudl probe placed its output — match that path).
5. Write the markdown report using the real data you captured.
6. pnpm typecheck.
7. Commit with Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com> trailer.
8. git push -u origin probe/ussf-learning-directory.
9. gh pr create with a title like probe: USSF Learning Center directory Phase 0 report and a body summarizing the finding + the escalation outcome.

**Final output (your last message to me):**

DONE: <PR URL>
Decision: <steps 1-3 yielded data | escalation to user required | blocked at CDN>
Key finding: <one sentence — e.g. "Angular shell has no inlined state; bundle references /api/v2/courses which 401s without auth">
If you get stuck or find something that invalidates the approach (e.g. the page 451s from the Anthropic egress IP), stop and report back with what you learned rather than improvising a Playwright dependency.