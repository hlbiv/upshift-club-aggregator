scraper/output/master.csv

# JSONL fallback files written by scrape_run_logger when the DB is
# unreachable. Drained into scrape_run_logs on the next successful
# connection and renamed to *.drained.jsonl for audit.
logs/

# Generated dedup-review CSVs from `--source club-dedup-resolve`. Each
# run writes a date-stamped file; these are operator review artifacts,
# not source.
dedup/output/dedup-review-*.csv

